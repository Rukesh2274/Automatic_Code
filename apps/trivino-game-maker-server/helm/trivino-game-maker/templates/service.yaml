apiVersion: v1
kind: Service
metadata:
  name: {{ include "trivino-game-maker.fullname" . }}
  labels:
    {{- include "trivino-game-maker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "trivino-game-maker.selectorLabels" . | nindent 4 }}
